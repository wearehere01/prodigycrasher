import webbrowser, pyautogui
def open_tab(url): # Url must be a string
        webbrowser.open(url)
        return "Tab Opened"

def close_tab():
    pyautogui.hotkey('ctrl', 'w')
    return "Tab Closed"
